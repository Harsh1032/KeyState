Once downloading the file two steps are necessary to acces the file.

on terminal cd client and do nmp install and similarly cd server and do npm install.

After that cd server and do npm start and for the client also do npm start it will start running the project.
